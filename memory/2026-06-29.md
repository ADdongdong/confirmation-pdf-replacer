# 2026-06-29 工作日志

## 函证 PDF 工具 — 新增加粗提示行

- 用户要求在项目联系人上方新增一行固定黑体加粗提示文字（不可修改）
- 内容：`若您方有相关本询证函函证事项问题，请直接联系下方项目联系人电话：`
- 样式：NSimSun 伪加粗（0.5pt 偏移双重渲染），字号与正文一致，首行 2 个 CJK 空格缩进

### 实现细节

- `process_page()` 签名新增 `contact_prompt=None` 参数
  - 在联系方式区域之前计算并渲染 prompt_blocks
  - 提示行使用 NSimSun + 0.5pt 右偏移双重渲染实现伪加粗
  - 提示行使用 `wrap_cjk_text()` 断行
- `process_from_data()` 构建 `PROMPT_LINE`（INDENT + 固定文案），传入 `process_page()`

### 变更记录（11:09）
- **移除 SimHei 黑体字体**：用户要求提示行与其他文字统一使用 NSimSun，仅做加粗效果
- **移除 `_find_bold_font()` 函数**，不再加载 SimHei.ttf
- **提示行改为纯伪加粗**：NSimSun 正常渲染 + 右偏移 0.5pt 叠加
- **联系方式字段加缩进**：项目联系人、项目联系人电话、收件人、收件人电话、邮箱 共 5 个字段均添加 `\u3000\u3000` 首行缩进

### 测试结果
- 10/10 全部成功
- 提示行在 y=188 处 `[B]` 伪加粗渲染
- 联系方式 5 行均带缩进：y=203→255
- 表格保护线 y=291，未受影响

### 变更记录（11:43）— 两栏布局 + 1.25倍行距
- **联系方式两栏布局**：
  - 第一排：`项目联系人：xxx`（左栏） | `项目联系人电话：xxx`（右栏）
  - 第二排：`收件人：xxx`（左栏） | `收件人电话：xxx`（右栏）
  - 第三排：`邮箱：xxx`（全宽单行）
  - 左栏保留 `\u3000\u3000` 缩进，右栏无缩进
  - 栏间距 20pt，左右栏各占 `(para_w - 20) / 2`
- **行间距 1.25 倍**：`contact_gap = body_fs * 1.25`，若空间不够则自动压缩
- **白化扩展**：`whiteout_bottom` 扩展到 `table_y - 3`，清除旧模板残留文字
- `process_from_data()` 中 contact_lines 改为 `[('pair', left, right), ('single', text)]` 元组列表
- `process_page()` 渲染时检测 tuple 类型分栏渲染，保留纯字符串向后兼容（话术文件路径）

### 测试结果
- 9/9 全部成功
- 两栏同排：左y=201 右y=201 ✓
- 行距：y=201→212=11.2pt, y=212→224=11.2pt (9pt*1.25=11.25)
- 白化：y=0→265，表格从 y=292 开始完好
